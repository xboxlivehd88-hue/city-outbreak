# CITY OUTBREAK — LEGACY HANDOFF POINTER

This file is retained only so older links do not break.

The authoritative current project handoff is:

- `CITY_OUTBREAK_HANDOFF.md`

Do not use older version numbers, gameplay assumptions, or tuning notes from this legacy file. Fetch current `main:index.html` and read `CITY_OUTBREAK_HANDOFF.md` before making changes.
